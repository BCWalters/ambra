{
  "name": "@ambra/e2e",
  "private": true,
  "version": "0.0.1",
  "description": "End-to-end reading-experience correctness suite (real headless Chromium via Playwright, driving the actual built extension) — deliberately separate from @ambra/engine's unit tests: this catches real layout/rendering/interaction regressions unit tests structurally cannot (see README.md).",
  "type": "module",
  "scripts": {
    "build:extension": "node scripts/build-extension.mjs",
    "test:e2e": "node scripts/build-extension.mjs && playwright test",
    "test:e2e:headed": "node scripts/build-extension.mjs && playwright test --headed",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^24.10.0",
    "typescript": "^6.0.0"
  }
}

/**
 * Resolves and loads spine items and their referenced resources (images,
 * embedded fonts, audio/video, SVG, MathML) from an `EpubContainer`. See the
 * `content-loader` and `font-obfuscation` work items for the implementation
 * of this class.
 */
export class ContentLoader {
  // TODO(content-loader): resolve manifest hrefs to container entries and
  // load spine XHTML plus referenced resources.
  // TODO(font-obfuscation): de-obfuscate embedded fonts per the IDPF
  // font obfuscation algorithm.
}

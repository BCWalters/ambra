{
  "name": "@ambra/engine",
  "private": true,
  "version": "0.0.0",
  "description": "Ambra's core EPUB3 engine: container/OPF/Nav parsing, layout, pagination, and CFI locators. Zero runtime dependencies — built entirely on native browser APIs (DecompressionStream, DOMParser).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:fixtures": "./scripts/build-fixtures.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.5.1",
    "happy-dom": "^20.14.5",
    "typescript": "^6.0.0",
    "vitest": "^5.0.0"
  }
}
